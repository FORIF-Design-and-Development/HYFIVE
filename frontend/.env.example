# Set to 'false' when deploying inside react-native-webview (disables iPhone mock frame)
VITE_USE_MOBILE_FRAME=true

# Backend API base URL (Spring Boot)
VITE_API_BASE_URL=http://localhost:8080

# Google OAuth 2.0 Client ID (Web application)
# GCP Console → APIs & Services → Credentials
VITE_GOOGLE_CLIENT_ID=

# Kakao Maps JavaScript Key
# Kakao Developers → 내 애플리케이션 → 앱 키 → JavaScript 키
# Register platform domains (localhost:5173, hyfive-forif.web.app) before use
VITE_KAKAO_MAP_KEY=

# Geoapify Static Maps API key (used only for the walk result snapshot)
# Sign up at https://www.geoapify.com — no credit card required, 3,000 req/day free
# MyProjects → API Keys → Create a new key
# Restrict to HTTP referrers: localhost:5173, hyfive-forif.web.app
VITE_GEOAPIFY_API_KEY=
